// Orbit 예산을 아직 읽지 않는 동안 쓰는 자리표시 값.
// 다음 단계에서 getOrbitSnapshot으로 대체한다. 위시·이벤트·수령 기록은
// 전부 orbital-wish에 저장되므로 여기에 샘플 데이터는 없다.

/** 이번 달 남은 자유비용 */
export const SAMPLE_FREE_AMOUNT = 92_400

/** 어제 마감된 기간에서 안 쓰고 남은 예산 */
export const SAMPLE_CARRYOVER = 12_400
