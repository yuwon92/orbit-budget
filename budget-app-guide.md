# 예산 앱 개발 가이드

개인용 예산 관리 웹앱. 항목별 예산을 설정해두고 달력으로 흐름을 보면서 "오늘 얼마 쓸 수 있는지" 판단하는 것이 목적.

---

## 1. 무엇을 만드는가

### 해결하려는 문제

기존 가계부 앱은 과거를 기록하는 데 집중한다. 한 달 뒤에 "식비가 많았네"를 알려주지만 오늘 점심에 얼마를 써도 되는지는 알려주지 않는다.

반대로 하루 예산만 계산해주는 앱은 항목별 관리가 없다. 식비 25만원, 교통비 2만원처럼 미리 정해두고 관리하고 싶어도 방법이 없다.

이 앱은 둘 다 한다.

### 핵심 화면 요소

숫자를 두 개 보여준다.

- **오늘 예산**: 아침에 정해지고 하루 종일 변하지 않는 기준선
- **남은 금액**: 지출할 때마다 실시간으로 줄어드는 값

기준선이 고정되어야 초과 여부를 인지할 수 있다. 잔여 금액만 있으면 그냥 줄어드는 숫자일 뿐이다. 초과하면 남은 금액을 음수로, 빨간색으로 표시한다.

### 만들지 않을 것

초기 버전에서 제외한다.

- 계좌 자동 연동 (오픈뱅킹 API는 개인 개발자 승인이 까다로움)
- 클라우드 동기화, 로그인, 서버
- 통계 리포트, 차트
- 다중 통화 (해외 구독은 원화 환산 금액을 직접 입력)

---

## 2. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | React + Vite | 자료가 많아 AI 코드 생성 품질이 좋음 |
| 언어 | TypeScript | 금액 계산에서 타입 실수를 막아줌 |
| 스타일 | Tailwind CSS | 클래스만으로 빠르게 조립 |
| 저장소 | Dexie.js (IndexedDB 래퍼) | localStorage는 용량 한계와 문자열 변환 문제가 있음 |
| 날짜 | date-fns | dayjs보다 트리셰이킹이 잘 됨 |
| 배포 | Vercel 또는 GitHub Pages | 무료, 자동 배포 |

### 초기 세팅

```bash
npm create vite@latest budget-app -- --template react-ts
cd budget-app
npm install
npm install dexie dexie-react-hooks date-fns
npm install -D tailwindcss @tailwindcss/vite
```

`vite.config.ts`에 Tailwind 플러그인을 추가하고, `src/index.css` 상단에 `@import "tailwindcss";`를 넣는다.

PWA로 만들어 홈 화면에 추가하려면 나중에 `vite-plugin-pwa`를 추가한다. 처음부터 넣을 필요는 없다.

---

## 3. 데이터 모델

### Category (카테고리)

```typescript
interface Category {
  id: string;
  name: string;           // "식비", "교통비", "카페"
  monthlyBudget: number;  // 월 예산. 0이면 예산 미설정
  color: string;          // 화면 표시용 hex
  isFixed: boolean;       // 고정비 여부. true면 자유 예산에서 제외
}
```

### Transaction (거래)

```typescript
interface Transaction {
  id: string;
  date: string;           // "2026-09-13" 형식
  amount: number;         // 지출은 양수, 수입은 음수로 저장하지 말 것
  type: 'expense' | 'income';
  categoryId: string | null;  // 수입이거나 미분류면 null
  memo: string;
  isPlanned: boolean;     // 예정 거래인지 실제 발생인지
}
```

`amount`는 항상 양수로 저장하고 `type`으로 방향을 구분한다. 음수를 섞으면 합계 계산할 때 실수하기 쉽다.

### RecurringRule (반복 거래)

```typescript
interface RecurringRule {
  id: string;
  name: string;           // "넷플릭스", "용돈"
  amount: number;
  type: 'expense' | 'income';
  categoryId: string | null;
  dayOfMonth: number;     // 매달 며칠
  startDate: string;
  endDate: string | null; // null이면 무기한
}
```

앱을 열 때 이번 달 반복 규칙을 확인해서 아직 생성되지 않은 예정 거래를 만든다.

### MonthSettings (월 설정)

```typescript
interface MonthSettings {
  yearMonth: string;      // "2026-09"
  reserveAmount: number;  // 예비비. 자유 예산에서 미리 떼어둘 금액
}
```

---

## 4. 계산 로직

여기가 앱의 핵심이다. 가장 먼저 만들고 가장 많이 테스트해야 한다.

### 이번 달 가용액

```
가용액 = 이번 달 총수입 - 이번 달 총지출(이미 발생한 것)
```

수입에는 확정된 예정 수입도 포함한다. 알바비처럼 날짜와 금액이 정해진 것은 아직 안 들어왔어도 계산에 넣는다.

### 오늘 예산

```
오늘 예산 = (가용액 - 남은 기간의 예정 지출 - 예비비) / 남은 일수
```

- **남은 기간의 예정 지출**: 오늘 이후 날짜에 잡힌 구독료, 계획 소비, 상환 등
- **남은 일수**: 오늘 포함, 이번 달 마지막 날까지
- **예비비**: 사용자가 설정한 금액. 0이면 계산에서 빠짐

이 값은 하루에 한 번, 날짜가 바뀔 때만 계산한다. 계산 결과를 저장해두고 그날 안에는 다시 계산하지 않는다.

### 남은 금액

```
남은 금액 = 오늘 예산 - 오늘 이미 쓴 금액
```

지출을 입력할 때마다 즉시 재계산한다. 음수가 될 수 있다.

### 항목별 진행률

```
진행률 = 이번 달 해당 카테고리 지출 합계 / 카테고리 월 예산
```

80%를 넘으면 주황색, 100%를 넘으면 빨간색으로 표시한다.

### 아낀 돈 처리

카테고리 예산을 다 쓰지 않아도 그 차액을 따로 이월하지 않는다. 위의 "오늘 예산" 공식이 실제 지출 기준으로 계산하기 때문에, 식비를 아끼면 자동으로 가용액이 늘어나고 오늘 예산이 올라간다. 별도 로직이 필요 없다.

### 주의할 점

**정수로 계산할 것.** 원화는 소수점이 없다. 나눗셈 결과는 `Math.floor()`로 내림 처리한다. 올림하면 실제보다 많이 쓸 수 있다고 착각하게 된다.

**날짜 경계.** `new Date()`는 로컬 타임존을 쓴다. 날짜 문자열을 만들 때 `toISOString()`을 쓰면 UTC로 변환되면서 하루가 밀릴 수 있다. date-fns의 `format(date, 'yyyy-MM-dd')`를 쓴다.

**월말 처리.** 반복 규칙의 `dayOfMonth`가 31인데 9월은 30일까지다. 이런 경우 해당 월의 마지막 날로 조정한다.

---

## 5. 화면 구성

### 홈

위에서부터 순서대로.

1. 오늘 날짜
2. 오늘 예산 (작게, 회색)
3. 남은 금액 (크게, 강조)
4. 진행률 바
5. 항목별 예산 카드 목록 (이름, 사용액/예산, 진행률 바)
6. 오늘 지출 내역
7. 지출 추가 버튼 (하단 고정)

### 달력

월 단위 그리드. 각 날짜 칸에 표시할 것.

- 그날 지출 총액
- 예정 거래가 있으면 점 표시
- 수입이 있는 날은 다른 색

날짜를 누르면 그날의 상세 내역이 아래에 펼쳐진다.

### 지출 입력

금액 입력이 가장 중요하다. 숫자 키패드가 바로 뜨도록 `inputMode="numeric"`을 설정한다.

- 금액 (자동으로 천 단위 콤마)
- 카테고리 선택 (버튼 나열, 드롭다운 금지)
- 날짜 (기본값 오늘)
- 메모 (선택)

입력 단계를 최소화한다. 금액 치고 카테고리 누르고 저장, 세 번 터치로 끝나야 한다.

### 설정

- 카테고리 관리 (추가, 수정, 예산 설정)
- 반복 거래 관리
- 예비비 설정
- CSV 내보내기

---

## 6. CSV 내보내기

나중에 지출 데이터를 분석에 쓸 수 있도록 처음부터 넣어둔다. 백업 수단이기도 하다.

컬럼 구성.

```
date,type,category,amount,memo,is_planned
2026-09-13,expense,식비,12000,점심,false
```

`Blob`과 `URL.createObjectURL`로 다운로드시킨다. 라이브러리가 필요 없다.

한글이 깨지지 않도록 BOM을 붙인다.

```typescript
const bom = '\uFEFF';
const blob = new Blob([bom + csvContent], { type: 'text/csv;charset=utf-8;' });
```

---

## 7. 개발 순서

한 번에 다 만들려고 하면 중간에 막힌다. 이 순서대로 하나씩 완성한다.

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| 1 | 프로젝트 세팅, Tailwind 적용 | 화면에 글자 하나 뜨면 성공 |
| 2 | Dexie 스키마 정의, 카테고리 CRUD | 카테고리 추가하고 새로고침해도 남아 있음 |
| 3 | 지출 입력과 목록 | 지출 넣으면 목록에 뜸 |
| 4 | 계산 로직 (별도 파일로 분리) | 콘솔에서 값 확인 |
| 5 | 홈 화면 | 두 숫자와 항목별 바가 표시됨 |
| 6 | 반복 거래 | 구독료가 자동으로 예정 거래에 들어옴 |
| 7 | 달력 뷰 | 월 전체가 한눈에 보임 |
| 8 | CSV 내보내기 | 엑셀에서 한글이 안 깨지고 열림 |
| 9 | 배포, PWA 설정 | 폰 홈 화면에서 실행됨 |

4번 계산 로직은 `src/lib/budget.ts` 같은 별도 파일에 순수 함수로 분리한다. UI와 섞으면 나중에 디버깅이 어려워진다.

---

## 8. 초기 데이터 (2026년 9월 기준)

앱을 처음 실행할 때 시드 데이터로 넣거나, 수동 입력 테스트용으로 쓴다.

### 카테고리

| 이름 | 월 예산 | 고정비 |
|---|---|---|
| 식비 | 258,000 | O |
| 카페 | 43,000 | O |
| 교통비 | 19,970 | O |
| 구독 | 58,490 | O |
| 미용 | 35,000 | O |
| 투자 | 20,000 | O |
| 기타 | 0 | X |

### 반복 거래

| 이름 | 금액 | 유형 | 날짜 |
|---|---|---|---|
| 용돈 | 700,000 | 수입 | 1일 |
| 구독 A | 9,900 | 지출 | 8일 |
| 구독 B | 2,500 | 지출 | 17일 |
| 구독 C | 1,100 | 지출 | 21일 |
| 구독 D | 13,990 | 지출 | 21일 |
| 해외 구독 | 31,000 | 지출 | 30일 |

### 9월 일회성 거래

| 날짜 | 내용 | 금액 | 유형 |
|---|---|---|---|
| 9/1 | 계획 소비 | 50,000 | 지출 |
| 9/10 | 알바비 | 821,950 | 수입 |
| 9/10 | 대여금 상환 | 500,000 | 지출 |
| 9/13 | 계획 소비 | 100,000 | 지출 |
| 9/25 | 지원금 | 119,000 | 수입 |

### 검산

이 데이터로 계산했을 때 나와야 하는 값.

```
총수입   = 700,000 + 821,950 + 119,000 = 1,640,950
고정지출 = 258,000 + 43,000 + 19,970 + 58,490 + 35,000 + 20,000 = 434,460
일회성   = 500,000 + 50,000 + 100,000 = 650,000
자유 예산 = 1,640,950 - 434,460 - 650,000 = 556,490
```

계산 로직을 만든 뒤 이 숫자가 나오는지 확인한다. 안 나오면 로직이 틀린 것이다.

---

## 9. AI에게 코드를 맡길 때

바이브 코딩으로 진행할 때 결과 품질을 높이는 방법.

**한 번에 하나씩 요청한다.** "앱 전체를 만들어줘"보다 "Dexie 스키마와 카테고리 CRUD 훅을 만들어줘"가 훨씬 잘 나온다. 위의 개발 순서 표를 그대로 요청 단위로 쓰면 된다.

**이 문서의 해당 섹션을 통째로 붙여넣는다.** 데이터 모델을 만들 때는 3번 섹션을, 계산 로직을 만들 때는 4번 섹션을 그대로 준다. 요약해서 설명하는 것보다 정확하다.

**에러는 전문을 그대로 붙여넣는다.** 요약하지 말고 콘솔에 뜬 것을 통째로 복사한다.

**동작하면 커밋한다.** 한 단계가 끝날 때마다 커밋해두면 다음 단계에서 망가뜨려도 되돌릴 수 있다.

```bash
git add .
git commit -m "카테고리 CRUD 완료"
```

**돈 계산은 직접 검산한다.** AI가 짠 계산 로직은 그럴듯해 보여도 틀릴 수 있다. 8번 섹션의 검산 값으로 반드시 확인한다.

---

## 10. 나중에 생각할 것

지금은 안 해도 되지만 언젠가 필요해질 수 있는 것들.

- 월 이월 처리 (남은 돈을 다음 달로 넘길지)
- 카테고리별 예산 자동 추천 (지난 3개월 평균)
- 데이터 백업과 복원 (JSON 내보내기/가져오기)
- 여러 달 데이터로 지출 예측

마지막 항목은 데이터가 반년쯤 쌓인 뒤에 별도 프로젝트로 다루는 편이 낫다.
