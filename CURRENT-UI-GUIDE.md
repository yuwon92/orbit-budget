# Orbit Budget — 현재 UI 구현 가이드

> 이 문서는 기획 문서가 아니라 **현재 코드에 실제로 구현된 상태**를 설명한다.  
> 다른 AI나 개발자가 저장소를 처음 열었을 때 화면 구조와 수정 지점을 빠르게 파악하기 위한 문서다.

## 1. 앱 요약

Orbit Budget은 “오늘 얼마를 쓸 수 있는지”를 가장 먼저 보여주는 개인 예산 관리 웹앱의 UI 프로토타입이다.

현재 단계의 핵심 목표는 완성도 높은 UI이며, 데이터 저장과 실제 계산 기능은 아직 연결하지 않았다. 화면에 표시되는 금액과 거래는 모두 `src/App.tsx` 안의 목업 데이터다.

### 현재 구현 상태

- React + TypeScript + Vite 기반 단일 페이지 앱
- 홈, 달력, 거래 내역, 설정의 4개 화면
- 데스크톱 사이드바와 모바일 하단 내비게이션
- 라이트/다크 테마 전환
- 지출 추가 바텀시트
- 반응형 레이아웃
- Lucide 아이콘
- CSS 기반 행성 그래픽과 카테고리 구슬
- 실제 DB, 로그인, 서버, 예산 계산, CSV 생성은 미구현

## 2. 실행 방법

```bash
npm install
npm run dev
```

기본 개발 주소:

```text
http://127.0.0.1:5173/
```

프로덕션 빌드 확인:

```bash
npm run build
```

## 3. 주요 파일

```text
day-budget-app/
├─ budget-app-guide.md       # 제품 기능 및 계산 로직 기획
├─ budget-app-ui-guide.md    # 원래 UI 디자인 원칙
├─ CURRENT-UI-GUIDE.md       # 현재 구현 상태 설명서
├─ package.json
├─ vite.config.ts
├─ index.html
└─ src/
   ├─ main.tsx               # React 진입점
   ├─ App.tsx                # 모든 화면, 컴포넌트, 목업 데이터, 상태
   └─ index.css              # 전체 디자인 시스템과 반응형 스타일
```

현재는 프로토타입 속도를 위해 대부분의 UI가 `App.tsx`에 모여 있다. 기능 개발을 시작할 때는 화면과 공용 컴포넌트를 분리하는 것이 좋다.

## 4. 앱 구조

별도의 라우터는 사용하지 않는다. `App` 컴포넌트의 `active` 상태에 따라 화면을 교체한다.

```text
App
├─ Header
├─ Sidebar                     데스크톱 전용
├─ main
│  ├─ HomeView
│  ├─ CalendarView
│  ├─ TransactionsView
│  └─ SettingsView
├─ Desktop Add Expense Button
├─ Bottom Navigation           모바일 전용
└─ ExpenseSheet                조건부 렌더링
```

### 전역에 가까운 로컬 상태

`App` 안에 다음 상태가 있다.

| 상태 | 타입 | 역할 |
|---|---|---|
| `active` | `'home' \| 'calendar' \| 'transactions' \| 'settings'` | 현재 화면 |
| `dark` | `boolean` | 다크 테마 여부 |
| `sheet` | `boolean` | 지출 추가 시트 표시 여부 |

테마 변경 시 `document.documentElement`에 `.dark` 클래스를 추가한다. 테마는 새로고침 후 유지되지 않는다.

## 5. 화면별 구성

### 5.1 홈 `HomeView`

정보 우선순위는 다음과 같다.

1. 오늘 남은 금액
2. 오늘 예산
3. 오늘 사용 진행률
4. 이번 달 카테고리 예산
5. 오늘 지출

구성:

- Hero card
  - 날짜: `9월 13일, 일요일`
  - 남은 금액: `32,400원`
  - 오늘 예산: `41,200원`
  - 사용 진행률: `40%`
  - 우측 CSS 행성 그래픽
- 이번 달 예산 카드 4개
- 오늘 지출 2개
- 지출 추가 버튼

Hero의 행성은 정적인 장식이다. 흔들림 문제 때문에 부유 애니메이션을 제거한 상태다.

### 5.2 달력 `CalendarView`

- 2026년 9월 고정 목업
- 월간 7열 그리드
- 날짜별 지출 합계
- 예정 거래: 파란 점
- 수입: 청록 점
- 선택 날짜: 라벤더 원형 배경
- 오늘과 선택 날짜를 구분하는 실제 로직은 아직 없음

표시 점은 **금액 바로 왼쪽**에 같은 줄로 배치한다.

```text
8
● 9,900
```

달력 아래에는 선택 날짜 요약 카드가 있다. 이전/다음 달 버튼은 UI만 존재하며 동작하지 않는다.

### 5.3 거래 내역 `TransactionsView`

- 검색 입력 UI
- 필터 버튼 UI
- CSV 내보내기 버튼 UI
- 날짜별 거래 그룹
- 지출과 수입의 방향 아이콘

검색, 필터, CSV 다운로드는 아직 동작하지 않는다.

### 5.4 설정 `SettingsView`

- 카테고리 관리
- 반복 거래
- 예비비 설정
- 화면 테마
- 데이터 내보내기

화면 테마 토글만 실제로 동작한다. 나머지는 확장용 UI 뼈대다.

### 5.5 지출 추가 `ExpenseSheet`

데스크톱에서는 우측 사이드 패널, 모바일에서는 하단 바텀시트로 표시된다.

현재 동작:

- 숫자 금액 입력
- 천 단위 콤마 표시
- 카테고리 선택
- 날짜 및 메모 입력 UI
- 금액이 없으면 저장 버튼 비활성화
- 저장 버튼 클릭 시 시트 닫힘

저장 버튼은 실제 거래 데이터를 추가하지 않는다.

## 6. 공용 시각 컴포넌트

### `Planet`

Home Hero와 데스크톱 사이드바 월간 요약에 사용한다.

- CSS 구체
- 파스텔 표면 패치
- 토성형 링
- 작은 기하학 장식
- Hero 버전과 `small` 버전 지원
- 애니메이션 없음

주의: Header의 Orbit 로고는 이 컴포넌트를 사용하지 않는다.

### Header Orbit 로고

`.logo-orbit` CSS로 만든 32px 원형 구슬이다.

- 링 없음
- 십자광 없음
- 청록 → 블루 → 라벤더 → 핑크 그라데이션
- 좌측 상단 흰색 반사광
- 약한 내부 음영과 그림자

### `CategoryPlanet`

이름은 이전 디자인에서 남은 것이지만 현재 형태는 행성이 아니라 **통일된 원형 구슬**이다.

- 모든 카테고리가 동일한 형태
- 카테고리별 색상만 변경
- 예산 카드, 오늘 지출, 지출 추가 시트에서 동일 컴포넌트 사용
- 기본 크기 28px

새로운 링, 위성, 카테고리별 모양 변형을 다시 추가하지 않는다.

## 7. 현재 목업 데이터

### 카테고리

`src/App.tsx`의 `categories` 배열에서 관리한다.

| 카테고리 | 사용액 | 예산 | 색상 |
|---|---:|---:|---|
| 식비 | 124,000 | 258,000 | `#8EBEFF` |
| 카페 | 31,000 | 43,000 | `#B7A7F8` |
| 교통비 | 18,600 | 19,970 | `#83DAD8` |
| 구독 | 27,490 | 58,490 | `#E5B4CF` |

구독은 카페와 혼동되지 않도록 더스티 핑크로 변경한 상태다.

### 오늘 거래

| 시간 | 내용 | 카테고리 | 금액 |
|---|---|---|---:|
| 12:30 | 점심 | 식비 | -12,000 |
| 16:20 | 아이스 라테 | 카페 | -4,500 |

## 8. 디자인 시스템

### 방향

```text
Clean financial UI 80% + pastel cosmic identity 20%
```

금융 정보가 항상 장식보다 우선한다. 픽셀/우주 표현은 로고, Hero 행성, 빈 상태 같은 브랜드 영역에만 사용한다.

### 핵심 컬러

| 토큰 | 라이트 기본값 | 역할 |
|---|---|---|
| `--planet-blue` | `#8EBEFF` | Primary |
| `--cosmic-cyan` | `#83E4E3` | Secondary |
| `--orbit-lavender` | `#B7A7F8` | Accent |
| `--stardust-pink` | `#E4B7E9` | Accent |
| `--star-yellow` | `#F7E58D` | 작은 하이라이트 |
| `--danger` | `#EF7777` | 초과/위험 |

### 필수 디자인 규칙

- 이모지를 사용하지 않는다.
- 기능 아이콘은 Lucide line icon을 사용한다.
- 금융 숫자는 일반 UI 폰트와 tabular number를 사용한다.
- 픽셀 폰트를 사용하지 않는다.
- 카테고리 아이콘은 모두 같은 구슬 형태를 유지한다.
- 모든 카드에 그라데이션이나 glow를 넣지 않는다.
- Hero 외 화면은 일반적인 금융 앱처럼 절제한다.
- 구독 색은 카페와 구분되는 더스티 핑크를 유지한다.

## 9. 반응형 동작

### 기본 데스크톱

- 상단 고정 Header: 72px
- 좌측 고정 Sidebar: 224px
- 본문은 Sidebar 너비만큼 왼쪽 여백
- 예산 카드: 4열
- 지출 추가: 우측 하단 floating button
- 지출 입력: 우측 패널

### `max-width: 900px`

- 예산 카드가 2열로 변경
- Hero 내부 간격과 행성 위치 축소

### `max-width: 680px`

- Header 높이 62px
- Sidebar 숨김
- 하단 내비게이션 표시
- Hero 숫자와 그래픽 축소
- 예산 카드 2열
- 지출 추가 버튼이 콘텐츠 내부에 표시
- 지출 입력이 바텀시트로 변경
- 달력 셀 높이와 금액 글자 축소

### `max-width: 400px`

- 예산 카드 1열
- 카드 내부가 가로 요약형으로 변경

## 10. 스타일 수정 시 주의사항

`src/index.css`는 초기 프로토타입을 빠르게 만든 결과 압축된 긴 규칙이 많고, 파일 하단에 최근 수정용 override가 추가되어 있다.

따라서 수정 시 다음 순서를 권장한다.

1. 먼저 파일 전체에서 같은 selector가 여러 번 등장하는지 검색한다.
2. 기존 규칙을 고칠 수 있으면 직접 수정한다.
3. 하단 override를 추가했다면 이전 override와 충돌하지 않는지 확인한다.
4. 데스크톱과 450×742 모바일 화면을 모두 확인한다.
5. `npm run build`를 실행한다.

기능 개발을 본격화할 때는 CSS를 토큰, 레이아웃, 컴포넌트, 화면, 반응형 파일로 분리하는 편이 좋다.

## 11. 기능 개발 시 권장 분리 구조

현재 UI를 유지하면서 기능을 붙일 경우 다음 구조를 권장한다.

```text
src/
├─ components/
│  ├─ layout/
│  │  ├─ Header.tsx
│  │  ├─ Sidebar.tsx
│  │  └─ BottomNav.tsx
│  ├─ budget/
│  │  ├─ CategoryBead.tsx
│  │  ├─ CategoryCard.tsx
│  │  └─ HeroBudgetCard.tsx
│  └─ transactions/
│     └─ ExpenseSheet.tsx
├─ pages/
│  ├─ HomePage.tsx
│  ├─ CalendarPage.tsx
│  ├─ TransactionsPage.tsx
│  └─ SettingsPage.tsx
├─ db/
│  └─ index.ts
├─ lib/
│  └─ budget.ts
├─ types/
│  └─ budget.ts
└─ styles/
   ├─ tokens.css
   ├─ components.css
   └─ responsive.css
```

UI를 분리할 때 시각 디자인을 동시에 바꾸지 않는다. 먼저 현재 모양을 그대로 옮긴 뒤 기능을 연결하는 것이 안전하다.

## 12. 아직 구현하지 않은 것

- Dexie/IndexedDB 저장
- 카테고리 CRUD
- 거래 생성, 수정, 삭제
- 실제 오늘 예산 계산
- 월 이동
- 날짜 선택에 따른 거래 목록 갱신
- 반복 거래 생성
- 예비비 저장
- 검색과 필터
- CSV 다운로드
- 테마 저장
- PWA
- 실제 현재 날짜 연동

현재 숫자를 실제 데이터처럼 오해하지 않도록 주의한다.

## 13. 다른 AI를 위한 작업 원칙

이 저장소를 수정하는 AI는 다음 순서를 따른다.

1. `budget-app-guide.md`에서 제품 기능을 확인한다.
2. `budget-app-ui-guide.md`에서 원래 디자인 원칙을 확인한다.
3. 이 문서에서 현재 구현과 최근 디자인 결정을 확인한다.
4. `src/App.tsx`와 `src/index.css`를 실제 코드 기준으로 다시 확인한다.
5. 요청 범위 밖의 목업 기능을 임의로 완성하지 않는다.
6. 기존 파스텔 금융 UI를 유지한다.
7. 변경 후 `npm run build`를 실행한다.
8. 가능하면 데스크톱과 모바일 브라우저에서 시각 검증한다.

### 최근 확정된 디자인 결정

- Header 로고는 링 없는 그라데이션 구슬이다.
- Hero의 큰 행성만 링을 유지한다.
- Hero 행성은 움직이지 않는다.
- 카테고리 아이콘은 모두 동일한 원형 구슬이다.
- 달력 상태 점은 금액 바로 왼쪽에 표시한다.
- 카페는 라벤더, 구독은 더스티 핑크다.

이 결정들은 사용자의 명시적 요청으로 확정된 것이므로, 새로운 요청 없이 이전 형태로 되돌리지 않는다.
