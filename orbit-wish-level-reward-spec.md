# Orbit Wish 레벨 보상·별가루 시스템 기획

## 1. 문서 목적

Orbit Wish에 레벨 보상, 별가루 경제, 상점, 랜덤박스, 보관함과 꾸미기 공간을 추가하기 위한 구현 기준을 정리한다.

기존 추천안 중 **우주 확장형 + 꾸미기 중심형 + 선택형 + 랜덤박스형**을 조합한다. 사용자가 소원을 위해 행동하고, 보상을 모아 자신의 우주를 꾸미는 순환을 만드는 것이 목표다.

```text
미션·연속 기록·위시 완주
        ↓
     XP + 별가루
        ↓
레벨 상승 → 별가루 + 확정·선택·상자 보상
        ↓
상점 직접 구매 / 코스믹 박스 구매
        ↓
       보관함
        ↓
  나의 우주에 장착·꾸미기
```

---

## 2. 핵심 원칙

- XP는 소비되지 않는 성장 수치다.
- 별가루는 획득하고 소비하는 게임 재화다.
- 별가루를 사용해도 XP와 레벨은 감소하지 않는다.
- 미션을 수령하면 XP와 별가루를 동시에 지급한다.
- 모든 레벨에서 별가루 또는 소유 가능한 아이템이 실제 보상으로 남아야 한다.
- 꾸미기 카테고리와 모든 희귀도는 Lv.1부터 확인하고 사용할 수 있다.
- 희귀 등급 자체를 레벨로 잠그지 않는다.
- 희귀성은 높은 가격, 상자 전용, 레벨 보상 전용, 지역 전용 같은 획득처로 만든다.
- 슬롯이나 지역처럼 구조를 해금하는 레벨에도 별가루나 아이템을 함께 지급한다.
- 상점 할인권은 사용하지 않는다. 재화 보상은 별가루로 직접 지급한다.
- 랜덤 보상만으로 진행을 강제하지 않는다. 원하는 아이템을 상점에서 직접 구매하는 경로를 함께 제공한다.

---

## 3. 성장 수치와 소유 자산

| 구분 | 성격 | 저장 방식 | 사용처 |
|---|---|---|---|
| XP | 누적 경험치 | 현재처럼 Wish 이벤트와 Claim에서 재계산 | 레벨 산정 |
| 별가루 | 소비 가능한 재화 | 획득·소비 원장을 DB에 저장하고 합계 계산 | 상점, 코스믹 박스 |
| 아이템 | 영구 소유 꾸미기 요소 | 보관함에 획득 기록 저장 | 나의 우주 꾸미기 |
| 상자 | 개봉 전까지 보유하는 보상 | 지급과 개봉 상태 저장 | 랜덤 아이템 획득 |
| 기능·지역 | 계정의 구조적인 확장 | 레벨 보상 수령 기록으로 판정 | 위시 슬롯, 꾸미기 지역 |

화면과 코드에서 경험치는 `XP`, 소비 재화는 `별가루`로만 표기한다. `STARDUST`를 XP의 영문 이름으로 다시 사용하지 않는다.

---

## 4. 미션 및 행동 보상

아래 수치는 1차 밸런스 기준이다. XP 배점은 기존 값을 유지하고 별가루 지급량을 추가한다.

| 행동 | XP | 별가루 | 지급 기준 |
|---|---:|---:|---|
| 하루 몫 일부 저금 | 5 | 2 | 위시별 하루 1회 |
| 하루 몫 완료 | 10 | 5 | 위시별 하루 1회 |
| 목표를 채우고 하루 더 기다리기 | 20 | 10 | 기다리기 이벤트 1회 |
| 남은 예산 넘기기 | 30 | 15 | 넘기기 이벤트 1회 |
| 7일 연속 달성 | 30 | 30 | 새로 완성된 7일 묶음마다 1회 |
| 위시 완주 | 100 | 50 | 위시별 최초 완주 1회 |
| 첫 위시 등록 | 50 | 20 | 계정 최초 1회 |

### 지급 규칙

- 미션 수령 한 번에 XP와 별가루를 함께 확정한다.
- 하루 몫은 여러 번 나눠 저금해도 위시별 하루 1회만 보상한다.
- 부분 저금 보상을 받은 뒤 같은 날 하루 몫을 완성했을 때 추가 차액을 줄지는 밸런스 구현 전에 별도로 확정한다.
- `carryover`와 직접 저금은 서로 다른 미션이므로 각각 보상할 수 있다.
- 앱 재실행, 중복 클릭, 저장 재시도에도 같은 원본 사건에서 별가루가 두 번 생기지 않아야 한다.

---

## 5. Lv.1~20 레벨 보상

아이템 이름은 제작 과정에서 바꿀 수 있는 작업명이다. 별가루 수량은 경제 테스트 후 조정할 수 있지만 보상 유형과 배치 원칙은 유지한다.

| 레벨 | 별가루 | 추가 보상 | 유형 |
|---:|---:|---|---|
| Lv.1 | 100 | 기본 행성·배경·링으로 구성된 `관측자 스타터 세트` | 시작 보상 |
| Lv.2 | 100 | 위시 슬롯 2 + 일반 꾸미기 아이템 3종 중 하나 선택 | 슬롯·선택형 |
| Lv.3 | 120 | 행성 색상 3종 중 하나 선택 | 선택형 |
| Lv.4 | 150 | 위시 슬롯 3 + 코스믹 박스 티켓 1장 | 슬롯·상자 |
| Lv.5 | 180 | 희귀 아이템 `별빛 궤도 링` | 확정 희귀 |
| Lv.6 | 200 | 우주 배경 3종 중 하나 선택 | 선택형 |
| Lv.7 | 220 | 동료 아이템 `꼬마 위성` | 확정 아이템 |
| Lv.8 | 250 | 행성 무늬 3종 중 하나 선택 + 코스믹 박스 티켓 1장 | 선택형·상자 |
| Lv.9 | 280 | 위성·동료 3종 중 하나 선택 | 선택형 |
| Lv.10 | 350 | 신규 지역 `달빛 전초기지` + 희귀 배경 `고요한 월면` | 지역·확정 희귀 |
| Lv.11 | 300 | 효과 아이템 `반짝이는 별가루` | 확정 아이템 |
| Lv.12 | 320 | 별빛 효과 3종 중 하나 선택 + 코스믹 박스 티켓 1장 | 선택형·상자 |
| Lv.13 | 500 | 별가루 보너스 | 재화 |
| Lv.14 | 380 | 희귀 아이템 하나가 반드시 나오는 `희귀 확정 상자` 1개 | 확정 희귀 상자 |
| Lv.15 | 450 | 희귀 동료 `작은 혜성` | 확정 희귀 |
| Lv.16 | 420 | 코스믹 박스 티켓 2장 | 상자 |
| Lv.17 | 450 | 희귀 효과 `혜성의 궤적` | 확정 희귀 |
| Lv.18 | 500 | 희귀 배경·링·동료 중 하나 직접 선택 | 희귀 선택형 |
| Lv.19 | 800 | 별가루 대량 보너스 | 재화 |
| Lv.20 | 800 | 신규 지역 `성운 정거장` + 전설 행성 스킨 + 프리미엄 박스 1개 | 지역·전설·상자 |

### 레벨 보상 처리 규칙

- 레벨별 별가루와 추가 보상은 각각 한 번만 받을 수 있다.
- 선택형 보상은 사용자가 선택을 완료할 때 수령 처리한다.
- 확정 아이템은 해당 레벨에 지정된 정확한 아이템을 지급한다.
- 상자 보상은 상자나 티켓을 지급한 시점과 실제 개봉 시점을 분리한다.
- 여러 레벨이 한 번에 오르면 도달한 모든 레벨의 보상을 누락 없이 표시한다.
- XP 배점이나 레벨 문턱이 바뀌어 현재 레벨이 다시 계산돼도 이미 받은 보상을 회수하거나 중복 지급하지 않는다.

---

## 6. 아이템 체계

### 꾸미기 분류

| 분류 | 예시 | 동시 장착 권장 수량 |
|---|---|---:|
| 행성 색상 | 노을빛, 심해빛, 보랏빛 | 1 |
| 행성 무늬 | 크레이터, 줄무늬, 결정 | 1 |
| 궤도 링 | 기본 링, 별빛 링, 초승달 링 | 1 |
| 배경 | 심우주, 월면, 성운 | 1 |
| 위성·동료 | 꼬마 위성, 작은 혜성, 우주선 | 1~3 |
| 효과·흔적 | 반짝임, 별똥별, 혜성 궤적 | 1 |

### 희귀도

| 희귀도 | 역할 |
|---|---|
| Common | 기본 색상과 작은 장식, 직접 구매 중심 |
| Rare | 눈에 띄는 링·동료·배경, 직접 구매와 상자 |
| Epic | 큰 시각 변화가 있는 배경·효과, 고가 구매와 희귀 상자 |
| Legendary | 레벨·지역 이정표를 대표하는 한정적인 보상 |

### 획득처

상점과 보관함에서 각 아이템의 획득처를 명확하게 표시한다.

| 획득처 | 의미 |
|---|---|
| 상점 구매 가능 | 별가루로 원하는 아이템을 직접 구매 |
| 코스믹 박스 | 일반 상자에서 확률적으로 획득 |
| 희귀 확정 상자 | 희귀 아이템 중 하나를 확정 획득 |
| 레벨 보상 전용 | 지정된 레벨에서만 획득 |
| 지역 전용 | 해당 지역을 얻은 뒤 구매 또는 획득 가능 |

- 희귀 상품도 Lv.1부터 상점이나 수집 목록에서 볼 수 있다.
- 상점 전용, 상자 전용, 레벨 전용 아이템을 구분한다.
- 지역 전용은 희귀도를 잠그는 것이 아니라 신규 지역과 함께 상품군이 추가되는 개념이다.
- 보유 아이템은 `보유 중`으로 표시하고 직접 구매를 막는다.
- 구매 전 실제 꾸미기 화면과 동일한 미리보기를 제공한다.

---

## 7. 상점과 가격 기준

가격은 고정 숫자보다 아이템 하나를 얻는 데 필요한 플레이 기간을 먼저 맞춘다.

| 상품 | 목표 획득 기간 |
|---|---|
| 일반 코스믹 박스 | 미션 3~5일 · 주 3회 구매 한도 |
| Common 직접 구매 | 미션 4~7일 |
| Rare 직접 구매 | 미션 2~3주 |
| Epic 직접 구매 | 미션 3~5주 |
| Legendary | 레벨·지역 이정표 또는 장기 수집 |

### 구매 규칙

- 별가루 잔액, 가격, 구매 후 잔액을 구매 확인 화면에 표시한다.
- 보유 중인 아이템은 다시 직접 구매할 수 없다.
- 별가루가 부족하면 구매를 시작하지 않는다.
- 별가루 차감과 아이템 지급은 하나의 DB 트랜잭션으로 처리한다.
- 구매 도중 앱이 종료돼도 별가루만 빠지거나 아이템만 생기는 상태가 없어야 한다.

---

## 8. 코스믹 박스

| 상자 | 구성 | 주요 획득 방식 |
|---|---|---|
| 일반 코스믹 박스 | Common·Rare 중심, 낮은 Epic 확률 | 별가루 구매, 레벨 티켓 |
| 희귀 확정 상자 | Rare 이상 1개 확정 | 주요 레벨 보상 |
| 프리미엄 박스 | Rare 이상 확정, Legendary 등장 가능 | Lv.20 등 큰 이정표 |

### 보호 규칙

- 희귀도별 등장 확률을 개봉 전에 공개한다.
- 미보유 아이템을 우선 지급한다.
- 불가피한 중복은 해당 등급 기준 별가루로 자동 전환한다.
- 일정 개봉 횟수 안에 Rare 이상을 보장하는 확정 규칙을 둔다.
- 레벨·지역 전용 아이템은 일반 상자에 넣지 않는다.
- 상자 결과는 뽑는 순간 DB에 먼저 확정한 뒤 연출한다.
- 개봉 연출 중 앱이 종료돼도 결과가 유실되거나 다시 뽑히지 않아야 한다.
- 상자는 유료 결제와 연결하지 않는다.

---

## 9. 화면과 진입 구조

하단 탭은 현재 4개를 유지한다. 기존 `관측자` 탭을 `관측소`로 확장하고 게임 요소를 전체 화면 하위 페이지로 구성한다.

```text
관측소 홈
├─ 나의 우주 미리보기
├─ 다음 레벨과 다음 주요 보상
├─ 별가루 잔액
├─ 미수령 레벨 보상
├─ 꾸미기
├─ 상점
└─ 기존 통계·칭호·설정
```

| 화면 | 핵심 기능 |
|---|---|
| 관측소 홈 | 꾸민 우주, 레벨, XP, 별가루, 다음 보상, 미수령 보상 표시 |
| 꾸미기·보관함 | 보유 아이템 선택, 실시간 미리보기, 장착·해제·저장 |
| 상점 | 상품 탐색, 희귀도·획득처·보유 여부 표시, 미리보기와 구매 |
| 상자 개봉 | 상자 선택, 확률 안내, 결과 확정, 획득 연출 |
| 레벨 보상 수령 | 확정 보상 확인, 선택형 보상 선택, 수령 완료 |

독립적인 `보상함` 탭은 만들지 않는다. 관측소 홈의 `미수령 보상` 카드에서 수령 화면으로 들어간다. 미수령 보상이 없으면 다음 레벨 보상 미리보기로 바꾼다.

상점과 꾸미기는 바텀시트가 아니라 전체 화면 하위 페이지로 연다. 상품 탐색과 실시간 미리보기에 충분한 공간을 확보하기 위함이다.

---

## 10. 추가 데이터

현재 `orbital-wish` DB에 아래 데이터가 필요하다. 새 스토어와 인덱스가 추가되므로 Dexie 스키마 버전을 올린다.

| 데이터 | 주요 필드 | 역할 |
|---|---|---|
| 아이템 카탈로그 | `id`, `name`, `category`, `rarity`, `source`, `price`, `asset` | 코드에 두는 정적 상품 정의 |
| 별가루 원장 | `id`, `type`, `amount`, `sourceType`, `sourceId`, `createdAt` | 획득·소비 내역과 잔액 계산 |
| 보유 아이템 | `itemId`, `acquiredAt`, `sourceType`, `sourceId` | 영구 소유 여부 |
| 장착 상태 | 카테고리별 `itemId`, `updatedAt` | 현재 나의 우주 구성 |
| 레벨 보상 수령 | `level`, `claimedAt`, `selectedItemId` | 중복 지급 방지와 선택 결과 보존 |
| 보유 상자 | `boxId`, `type`, `acquiredAt`, `sourceId`, `openedAt` | 지급과 개봉 시점 분리 |
| 상자 개봉 기록 | `id`, `boxId`, `itemId`, `duplicateDust`, `openedAt` | 결과 복구와 중복 전환 기록 |

별가루 잔액은 별도 숫자를 덮어쓰지 않고 원장 합계로 계산한다. 지급과 소비는 관련 데이터 변경과 함께 하나의 트랜잭션으로 처리한다.

### 원본 사건 ID 예시

| 사건 | `sourceId` 예시 |
|---|---|
| 미션 수령 | `mission:2026-09-09:share-{wishId}` |
| 위시 완주 | `complete:{wishId}` |
| 레벨 보상 | `level:14` |
| 상점 구매 | `purchase:{purchaseId}` |
| 상자 중복 전환 | `duplicate:{boxOpenId}` |

---

## 11. 기존 사용자와 예외 처리

- 업데이트 당시 레벨이 2 이상인 사용자는 현재 레벨까지의 보상을 소급해서 받을 수 있게 한다.
- 소급 보상은 관측소의 미수령 보상 목록에 표시하며 선택형 보상을 자동 선택하지 않는다.
- Lv.1 스타터 세트는 기존 사용자에게도 자동으로 보유 처리한다.
- 이미 열린 위시 슬롯은 그대로 유지한다.
- 장착한 아이템이 삭제되거나 카탈로그에서 사라지면 기본 아이템으로 안전하게 대체한다.
- 선택형 보상을 고르기 전 앱을 닫아도 다시 선택할 수 있어야 한다.
- 여러 레벨이 한 번에 상승해도 중간 레벨 보상이 누락되지 않아야 한다.

---

## 12. 권장 구현 순서

### 1단계 — 성장·재화 기반

- XP와 별가루 데이터·문구 완전 분리
- 별가루 원장과 잔액 계산
- 미션·완주·연속 기록의 별가루 동시 지급
- 레벨 보상 수령 기록과 기존 사용자 소급 처리

### 2단계 — 아이템과 꾸미기

- 정적 아이템 카탈로그
- 보관함과 장착 상태
- 관측소 홈과 나의 우주 미리보기
- 꾸미기 전체 화면과 실시간 미리보기

### 3단계 — 상점

- 상품 목록과 획득처 표시
- 구매 미리보기 및 확인
- 별가루 차감과 아이템 지급의 트랜잭션 처리

### 4단계 — 레벨 보상

- Lv.1~20 보상표 적용
- 선택형 보상 화면
- 확정 아이템 및 신규 지역 지급
- 다음 주요 보상 미리보기와 미수령 표시

### 5단계 — 랜덤박스

- 일반·희귀 확정·프리미엄 상자
- 확률, 미보유 우선, 중복 전환, 확정 규칙
- 개봉 결과 저장과 연출

### 6단계 — 밸런스 및 QA

- 하루·일주일·한 달 기준 별가루 획득량 검산
- 상점 및 상자 가격 조정
- 여러 레벨 동시 상승과 소급 보상 검증
- 새로고침·강제 종료·중복 클릭 시 이중 지급 방지
- 기존 사용자 DB 마이그레이션 검증

---

## 13. 완료 기준

- 모든 XP 표기는 경험치만 의미하고 별가루와 혼용되지 않는다.
- 미션 수령 한 번에 XP와 별가루가 정확히 한 번 지급된다.
- Lv.1~20의 모든 레벨에서 별가루 또는 소유 가능한 보상이 실제로 남는다.
- 꾸미기 카테고리와 희귀 등급을 레벨 해금만으로 제한하지 않는다.
- 희귀 아이템은 비싼 직접 구매, 상자, 확정 보상 등 서로 다른 획득처를 가진다.
- 사용자는 상점에서 원하는 아이템을 직접 살 수도 있고 코스믹 박스를 선택할 수도 있다.
- 구매·보상·상자 처리는 새로고침이나 재시도에도 중복되거나 유실되지 않는다.
- 획득한 아이템을 보관함에서 장착하고 관측소의 나의 우주에서 즉시 확인할 수 있다.
